# Publish only — do NOT source for `coderouter serve`.
# Use only with `op run --env-file=.env.publish.tpl -- uv publish ...`.
export TESTPYPI_TOKEN=op://Personal/CodeRouter Publish Tokens/TESTPYPI_TOKEN
export PYPI_TOKEN=op://Personal/CodeRouter Publish Tokens/PYPI_TOKEN
