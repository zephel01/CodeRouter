feat: v0.3.0 Claude Code 実運用向け品質改善

v0.2 完了後に Claude Code + qwen2.5-coder:14b で実運用して浮いた
「仕様通りには動いているが実モデルが歪んだ出力を返したときに壊れる」
領域を集中改修。

- v0.3-A Tool-call repair (non-streaming) -- translation/tool_repair.py
  * 上流モデルが tool_calls フィールドを使わず text 本文に
    {"name": ..., "arguments": ...} を書いてくる失敗パターンに対応
  * balanced-brace scanner (文字列/エスケープ認識) で text body から JSON 抽出
  * ```json フェンスブロックも検出
  * リクエストが宣言した tool 名 allowlist で照合、未知 tool 名は text に残す
  * 抽出 JSON は OpenAI tool_calls 形式に正規化 → Anthropic tool_use block 変換
  * to_anthropic_response(..., allowed_tool_names=[...]) で呼び出し

- v0.3-B Mid-stream fallback guard -- routing/fallback.py + ingress
  * 新例外 MidStreamError(provider, original) を追加
  * FallbackEngine.stream() は first byte 送出後に AdapterError が出たら
    次 provider に fall through せず MidStreamError を raise
  * _anthropic_sse_iterator が MidStreamError を捕まえて
    event: error / type: api_error を emit して SSE を閉じる
  * 「最初の 1 byte も出せない」 overloaded_error とは明確に区別
  * 目的: Claude Code に部分応答 + 重複コンテンツが届く事故を防ぐ

- v0.3-C Usage aggregation -- translation/convert.py + adapters/openai_compat.py
  * stream 終端の message_delta.usage.output_tokens を正しく埋める
  * 優先順位: upstream completion_tokens (authoritative)
             > (emitted_chars + 3) // 4 概算
  * input_tokens は upstream が prompt_tokens を送ってきた場合のみ反映
  * OpenAI-compat adapter は streaming 時に
    stream_options.include_usage=true を自動付与
    (provider 側が extra_body で上書きしていればそちらが優先)
  * Ollama のように flag を無視する upstream でも char 概算で 0 にはならない

- v0.3-D Tool-call repair (streaming) strategy 2: downgrade to non-stream
  * tools を宣言した streaming リクエストは内部で stream=false に切り替え、
    v0.3-A の repair を通してから
    synthesize_anthropic_stream_from_response で
    Anthropic SSE イベント列を合成して返す
  * クライアントから見た wire はあくまで streaming
    (message_start → ... → message_stop)
  * tool を含まない streaming は従来通り真の streaming パス
  * トレードオフ: tool ターンは first-byte latency が full response 時間まで伸びる
    (tool ターンは実質的に「完成してから次の手」が前提なので許容)

- Bug fix (v0.3-E 実機疎通中に発見): Message.content=None pydantic reject
  * Claude Code は multi-turn 履歴に「tool_use のみ / text なし」の
    assistant ターンを含めるため、2 ターン目以降で必ず 500 に落ちていた
  * OpenAI spec は tool_calls を持つ assistant message に content: null を許可
  * Message モデルの content 型を str | list[dict[str, Any]] | None = None に拡張
  * _prepare_messages は exclude_none=True で dump するので
    upstream には content キーごと送らず OpenAI spec どおりの shape を維持

Tests: 54 (v0.2) → 87 (v0.3, +33)
  * test_tool_repair.py 13 件新設 (text 埋込 JSON 抽出の全パターン)
  * test_translation_anthropic.py +9 件
    (repair 連携 3 / usage 集計 5 / synthesizer 3 / content=None regression 1 - 重複 2 を除外)
  * test_ingress_anthropic.py +4 件
    (mid-stream error / tool 付き stream downgrade / tool なし real stream / 502 surface)
  * test_fallback.py +2 件
    (mid-stream で MidStreamError, 初期エラーは従来どおり fallback)
  * test_openai_compat.py +2 件
    (stream_options.include_usage 自動付与 / extra_body 上書き尊重)

Verified (実機, 2026-04-20):
  Ollama + qwen2.5-coder:14b + Claude Code で疎通確認。
  * (a) tool なし text streaming: real path で usage の
    upstream authoritative / char estimate fallback 両パスを実機確認
  * (b) Claude Code + tool 付き streaming: downgrade path で
    tool_use block が Claude Code UI に tool invocation として描画
  * (c) mid-stream guard: unit test でカバー
    (実機 pkill timing は困難のため optional とした)
  * Claude Code は毎ターン全 tool を宣言するため、
    Claude Code 経由では常に v0.3-D downgrade path に入る構造が判明

lint (ruff): v0.3 で導入した issue は 0。残 11 件はすべて v0.1/v0.2 由来。
