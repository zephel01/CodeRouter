feat: v0.2.0 Anthropic ingress

- POST /v1/messages (Anthropic Messages API 互換)
  * 非 streaming + streaming SSE (message_start → content_block_* → message_delta → message_stop)
  * tool_use / tool_result / image 含む content block の双方向変換
  * system string / system block list の flatten
  * stop_sequences / temperature / top_p / top_k passthrough
  * anthropic-version ヘッダは debug ログのみ (enforce はしない)
  * profile 選択は body > X-CodeRouter-Profile > default の順
  * 未知 profile は 400、全滅は 502 (非 stream) / event: error (stream)

- coderouter/translation/
  * anthropic.py: Anthropic wire-format の pydantic models (request/response/stream + content block 4 種)
  * convert.py: Anthropic ⇄ internal ChatRequest/ChatResponse の双方向変換
    - to_chat_request: Anthropic → ChatRequest
    - to_anthropic_response: ChatResponse → Anthropic
    - stream_chat_to_anthropic_events: stateful stream 変換 (block index 管理、text→tool_use 切替時は text を先に閉じる、multi tool_call に個別 index)
  * malformed tool_call JSON は _raw 退避で素通し、後段で修復可能に

- coderouter/ingress/app.py
  * anthropic_router を /v1 配下に include
  * / と HEAD / に tiny handler を追加 (Claude Code 起動時の preflight で 404 を返さない)

- providers.yaml
  * ollama-qwen-coder-14b: timeout_s 120 → 300 (Claude Code 15-20K token system prompt を 14B で処理する実測から)

- tests (+28 件、総数 54)
  * test_translation_anthropic.py 17 件: request/response/stream の変換ユニット
  * test_ingress_anthropic.py 11 件: HTTP 境界、profile 経路、SSE イベント順序、エラーマッピング

- plan.md
  * §8 v0.2 を完了形に更新 (DoD 全 checked、実装知見 8.4 に 7 項目)
  * §8.5 に v0.3 以降へ送った項目 (Anthropic native adapter / tool-call repair / mid-stream fallback guard / usage 集計 / Claude Code 専用 profile sample)
  * §6.1 v0.2 行を ✅ 完了に

疎通確認済み (2026-04-20):
  ANTHROPIC_BASE_URL=http://localhost:8088 claude
    → text 応答 / streaming SSE 順序 / tool 定義引き渡しまで一周
  tool_calls の構造化出力は qwen2.5-coder:14b 側の能力限界で text に落ちるケースあり。
  これは翻訳バグではなくモデル挙動なので v1.0 の tool-call repair で扱う。
